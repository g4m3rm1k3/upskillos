import { extras } from './notebooks.js'

export const lessons = [
  {
    id: 'l06-roles',
    title: '06.1 · Three roles for data',
    sections: ['A score is a claim about the future', 'Train, validate, test', 'Every look spends information', 'Sizes and trade-offs', 'Put it into practice'],
    skill: 'Assign data to training, validation and test roles and explain what each score can and cannot tell you.',
    prerequisite: 'Labs 01–05; train/validation splits from Lab 01 and uncertainty from Lab 05.',
    paragraphs: [
      'An evaluation score answers one question: **how will this model perform on the data it will meet after deployment?** Any row that influenced the model — its parameters, its preprocessing, or your choice among models — is no longer a fair sample of that future. Most evaluation mistakes are rows quietly playing two roles.',
      'The **training set** fits parameters. The **validation set** compares choices: model families, features, learning rates, regularization. The **test set** is opened once, at the end, to estimate performance of the single final choice. Training error measures fit; validation error guides decisions; test error is the report.',
      'Each time you look at validation error and change something, a little information about the validation rows flows into your model. After dozens of decisions, the validation score is optimistic too. That is why a separate test set exists — and why looking at it repeatedly turns it into a second validation set.',
      'A common split is 60/20/20, but the right sizes depend on the data. The validation and test sets must be large enough that their scores are not dominated by noise: with 100 test rows, an accuracy has a standard error around 4–5 points (Lab 05). With little data, use cross-validation for choices (next lesson) and keep a small final test set anyway.',
      'Procedure: decide the deployment question; set aside the test set first, before exploring; never compute anything on it until the end; do all fitting and selection with training and validation data; write down the final choice **before** opening the test set; report the test score with its uncertainty.',
    ],
    formula: 'train → fit parameters     validation → choose     test → report once',
    experiment: 'Open leak 4. With 1 model, compare the test score and the fresh score. With 50 and 200 models, compare again. Explain why "choosing on the test set" makes the report optimistic even though no model was trained on it.',
    question: 'A dataset of 1,000 rows is split 60/20/20 into train, validation and test. How many rows are in the validation set?',
    answer: 200,
    misconceptions: [{ answer: 600, feedback: 'That is the training set (60%). Validation is 20%.' }],
    explanation: '20% of 1,000 = 200 validation rows (600 train, 200 test).',
    reflection: 'For your project, what exactly is the future data your model will face? Which rows would you set aside as the test set, and why those?',
  },
  {
    id: 'l06-cv',
    title: '06.2 · Cross-validation',
    sections: ['One split is one draw', 'k folds', 'Report the spread, not just the mean', 'Choosing k', 'Cross-validation is for choosing'],
    skill: 'Implement k-fold cross-validation and interpret the mean and spread of fold scores.',
    prerequisite: 'Lesson 06.1; standard errors from Lab 05.',
    paragraphs: [
      'With 60 rows, a single 80/20 split validates on 12 rows. A different random 12 could change the score by 10 points. A single split gives one noisy measurement; you would not trust one noisy measurement of anything else.',
      '**k-fold cross-validation** shuffles the rows, cuts them into k nearly equal folds, and runs k experiments: each fold serves once as validation while the other k − 1 train the model. Every row is validated exactly once, and every model is trained on a fraction (k − 1)/k of the data.',
      'Report the mean fold score **and** its spread. The folds share training rows, so they are not independent, and the standard deviation of fold scores underestimates true uncertainty a little — but a large spread is a clear warning that the estimate is unstable. The playground shows fold-level bars for exactly this reason.',
      'k = 5 or 10 are common. Larger k trains on more data per fold (less pessimistic) at k times the cost. Leave-one-out (k = n) is nearly unbiased but noisy and expensive. For stable comparisons between models, use the **same folds** for every model, so fold-to-fold noise cancels in the difference.',
      'Cross-validation estimates how well a **procedure** performs; it produces k models, not one. After choosing, refit the chosen procedure on all training data. Cross-validation replaces a validation set; it does not replace the final untouched test set.',
    ],
    formula: 'CV score = (1/k) Σ_f score(model trained on all folds ≠ f, evaluated on fold f)',
    experiment: 'In leak 1, look at the honest fold bars. How much do individual folds disagree? Change the seed several times: how much does the honest mean move?',
    question: 'You run 5-fold cross-validation on 100 rows. How many rows train the model in each fold?',
    answer: 80,
    misconceptions: [{ answer: 20, feedback: 'That is the size of the validation fold. The other four folds train the model.' }],
    explanation: 'Each fold holds out 100/5 = 20 rows for validation, so 80 train. Every row is validated exactly once across the 5 folds.',
    reflection: 'Which model choice in your work was made from a single split? Would cross-validation have changed your confidence in it?',
  },
  {
    id: 'l06-baselines',
    title: '06.3 · Baselines: what counts as good?',
    sections: ['A number needs a comparison', 'Simple baselines', 'Domain baselines', 'Skill relative to a baseline', 'When the baseline wins'],
    skill: 'Choose appropriate baselines and express a model’s performance relative to them.',
    prerequisite: 'Lessons 06.1–06.2; the mean baseline from Lab 01.',
    paragraphs: [
      'A classifier with 94% accuracy sounds excellent — until you learn that 94% of transactions are legitimate. Predicting "legitimate" for everything achieves the same accuracy while catching no fraud. No score means anything without a comparison.',
      'Every problem has a trivial baseline: the **training mean** for regression; the **majority class** for classification; the **last observed value** (persistence) for time series. Compute it with the same split and metric as your model. If your model cannot beat it, it has learned nothing useful.',
      'Stronger baselines come from the domain: last week\'s value at the same hour, the rule an expert already uses, the vendor\'s estimate, a one-feature linear model. A new model should beat the best simple thing someone would otherwise use; otherwise its complexity is cost without benefit.',
      'Report **skill** relative to a baseline: `1 − error_model / error_baseline`. Skill 0 means no better than the baseline, 1 means perfect, negative means worse. For MSE against the mean baseline this is the familiar R² on held-out data.',
      'Sometimes the baseline wins, and that is a valuable finding: the signal may be absent, the features may not exist at prediction time, or the data may be too small. Recording failed experiments with their baselines prevents the next person from repeating them.',
    ],
    formula: 'skill = 1 − error_model / error_baseline     (MSE vs mean baseline → out-of-sample R²)',
    experiment: 'In leak 1, what accuracy does always predicting one class achieve on these balanced labels? Compare the honest CV score with that baseline.',
    question: '90% of emails in a dataset are not spam. What accuracy does the majority-class baseline achieve? (Answer as a decimal.)',
    answer: 0.9,
    explanation: 'Always predicting "not spam" is right on the 90% of emails that are not spam: accuracy 0.9, with zero spam caught. Lab 09 introduces metrics that expose this.',
    reflection: 'What is the strongest simple baseline for your prediction problem? Write how you would compute it without leakage.',
  },
  {
    id: 'l06-leakage',
    title: '06.4 · Leakage through preprocessing and selection',
    sections: ['A perfect score on noise', 'Why it happens', 'Pipelines fit inside the fold', 'Target and future leakage', 'An audit checklist'],
    skill: 'Detect leakage in an evaluation and repair it by fitting every data-dependent step inside each training fold.',
    prerequisite: 'Lessons 06.1–06.3; imputation and scaling from Lab 02.',
    paragraphs: [
      'The playground\'s first experiment gives 60 rows random yes/no labels and 500 random features. No real relationship exists, so honest accuracy should hover around 50%. Yet selecting the 10 features most correlated with the label and then cross-validating reports 75–90%. Nothing was trained on the validation rows — and the score is still false.',
      'With 500 candidates, some features correlate with the labels by pure chance across the 60 rows. Choosing them used every row\'s label, including the rows each fold later validates on. The chosen features are exactly those that happen to fit the validation rows. Information leaked through the **selection** step.',
      'Anything learned from data is part of the model: feature selection, scaling statistics, imputation values, vocabulary lists, target encodings, even the choice of which outliers to drop. Put all of it in a **pipeline** that is fit on each training fold and only applied to the validation fold. The honest bars in the playground do exactly this and fall back to chance — sometimes even below 50%, because holding out a row pulls its own class\'s training mean slightly away from it, a known quirk of small, balanced, pure-noise data that honest evaluation reveals rather than hides.',
      'Two more common leaks. **Target leakage**: a feature that is a consequence of the target, like "refund issued" when predicting fraud. **Future leakage**: a feature computed with information from after the prediction time, like a 7-day average that includes the next 3 days. Both look great offline and are unavailable or meaningless in deployment.',
      'Audit questions for any evaluation: Was anything computed on the full data before splitting? Could any feature be unknown at prediction time? Can the same entity (user, machine, project) appear on both sides? Were many models compared on the same held-out rows? Is the score suspiciously close to perfect? A surprising result deserves a leakage hunt before a celebration.',
    ],
    formula: 'for each fold: fit(selection, scaling, imputation, model) on train → apply to validation',
    experiment: 'In leak 1, raise p from 20 to 1,000 with k = 10. Record the leaky and honest scores at several points. Then set k = 1. Explain every trend you see.',
    question: 'Labels are assigned by a fair coin and are independent of every feature. What accuracy should an honest evaluation expect for any classifier? (Decimal.)',
    answer: 0.5,
    misconceptions: [{ answer: 0.9, tolerance: 0.05, feedback: 'A score that high on coin-flip labels is what the leaky evaluation reports. An honest evaluation cannot beat chance.' }],
    explanation: 'With labels independent of the inputs, no rule does better than chance on new rows: expected accuracy 0.5. A higher cross-validated score on such data is evidence of leakage or luck.',
    reflection: 'Which preprocessing steps in your own code are fit on data? Where exactly would each go inside a cross-validation loop?',
  },
  {
    id: 'l06-splits',
    title: '06.5 · Grouped and temporal splits',
    sections: ['Rows that are not independent', 'Split by group', 'Time only moves forward', 'Forward chaining', 'Match the split to deployment'],
    skill: 'Choose and implement group and time-ordered splits that match how a model will be used.',
    prerequisite: 'Lessons 06.1–06.4.',
    paragraphs: [
      'Ten readings from the same machine share that machine\'s quirks. If some readings train the model and others validate it, the model can succeed by recognizing the machine rather than learning anything that transfers. The playground\'s 1-nearest-neighbour model does exactly that: excellent on a random split, poor on unseen machines.',
      '**Group k-fold** assigns whole groups to folds, so no group appears on both sides. The group is the unit you will predict for in deployment: patients, users, machines, projects, documents from the same source. If the model will only ever see known machines, a random split may be fine — decide from the deployment question, not from convenience.',
      'Time-ordered data adds a constraint: the future cannot inform the past. A random split lets the model interpolate between neighbouring times, a luxury it will never have in deployment, where every prediction is about the future.',
      '**Forward chaining** (expanding-window validation) trains on everything up to time t and validates on the next block, then moves forward. Leave a **gap** between training and validation equal to your prediction horizon if the target is only known after a delay. `sklearn.model_selection.TimeSeriesSplit` implements the basic version.',
      'Procedure: write down what a single future prediction looks like (which entity, at what time, knowing what); build the split that reproduces that situation; build baselines and models only from information available at that moment. The split is part of the problem definition, not a detail.',
    ],
    formula: 'group split: groups(train) ∩ groups(val) = ∅     time split: max t(train) < min t(val)',
    experiment: 'Compare leak 2\'s four numbers. Which model would you choose from the random split? From the group split? Then check leak 3\'s ratio and explain why it is so large.',
    question: '100 time-ordered rows. Forward chaining starts with 50 training rows and validates on consecutive blocks of 10. How many folds fit?',
    answer: 5,
    explanation: 'Rows 50–99 remain after the first training window: 50 / 10 = 5 validation blocks, each trained on everything before it.',
    reflection: 'What is the group, and what is the time order, in your data? Which split matches how your model will be used?',
  },
  {
    id: 'l06-discipline',
    title: '06.6 · Test-set discipline and the winner’s curse',
    sections: ['The maximum of noisy scores', 'Multiple comparisons', 'Locking the test set', 'Reporting honestly', 'Demonstrate understanding'],
    skill: 'Explain why selection inflates the best score and design an evaluation protocol that avoids it.',
    prerequisite: 'Lessons 06.1–06.5.',
    paragraphs: [
      'Twenty models, each truly 70% accurate, are scored on the same 100 test rows. Their scores scatter around 70% by chance, and the highest might read 78%. Pick it, re-test on fresh data, and it returns to about 70%. Selecting the maximum of noisy measurements selects luck — the **winner\'s curse**.',
      'The chance of a spurious "win" grows with the number of comparisons. If each useless model has a 5% chance of passing a significance-style bar, the chance that at least one of 20 passes is `1 − 0.95²⁰ ≈ 0.64`. Hyperparameter searches, feature trials and repeated reruns all count as comparisons, whether or not anyone writes them down.',
      'Protocol: split off the test set first; do every comparison with cross-validation or a validation set; freeze the full pipeline, including preprocessing and threshold; run it once on the test set; report that number whatever it is. If you must revisit the test set, say so, and treat the new result as validation evidence.',
      'Report the test score with an interval (Lab 05), the baseline on the same rows, the split method and why it matches deployment, and how many configurations were tried. A modest honest score is worth more than an impressive one no one can reproduce.',
      'You have completed this lab when you can audit an experiment for the four leaks in the playground, repair each one, choose a split that matches deployment, and explain why the repaired score is lower and more trustworthy. The Python challenge asks you to build the splitters and an honest cross-validation with in-fold feature selection.',
    ],
    formula: 'P(at least one of m useless models passes at level α) = 1 − (1 − α)ᵐ',
    experiment: 'Set 1, 20 and 200 models in leak 4 and record the optimism each time, across three seeds. How does the winner’s inflation grow with the number of models?',
    question: 'Each of 20 useless models independently has a 5% chance of looking significant. What is the probability that at least one does? (Three decimals.)',
    answer: 0.6415, tolerance: 0.0006,
    misconceptions: [{ answer: 0.05, tolerance: 0.001, feedback: 'That is the chance for one model. With 20 independent tries, compute 1 − 0.95²⁰.' }, { answer: 1, tolerance: 0.001, feedback: 'Twenty tries at 5% do not add up to certainty: 1 − 0.95²⁰.' }],
    explanation: '1 − 0.95²⁰ = 1 − 0.3585 ≈ 0.642. With 20 tries, a spurious success is more likely than not.',
    reflection: 'How many configurations did you try before your last reported result? Rewrite the report to disclose that and its effect.',
  },
]

export const sources = [
  { title: 'scikit-learn · Cross-validation: evaluating estimator performance', url: 'https://scikit-learn.org/stable/modules/cross_validation.html' },
  { title: 'scikit-learn · Common pitfalls and recommended practices (data leakage)', url: 'https://scikit-learn.org/stable/common_pitfalls.html' },
  { title: 'ISL with Python · Chapter 5.1 and the wrong way to do cross-validation (7.10.2 in ESL)', url: 'https://www.statlearning.com/' },
  { title: 'Kapoor & Narayanan · Leakage and the reproducibility crisis in ML-based science', url: 'https://reproducible.cs.princeton.edu/' },
]

// Runnable cells and math ↔ code tables for each lesson live in notebooks.js.
for (const lesson of lessons) Object.assign(lesson, extras[lesson.id])
