export const lessons = [
  {
    id: 'l58-policy',
    title: '58.1 · Learning the policy directly',
    sections: ['Value-based versus policy-based', 'Stochastic policies', 'The objective', 'Continuous states and actions', 'Put it into practice'],
    skill: 'Formulate a parameterized stochastic policy and its expected-return objective.',
    prerequisite: 'Reinforcement learning (Lab 37), online learning (Lab 50), logistic regression (Lab 08), backpropagation (Lab 20).',
    paragraphs: [
      'Q-learning (Lab 37) learns action values and acts greedily on them. **Policy-gradient** methods instead parameterize the policy itself, π_θ(a | s), and adjust θ to increase expected return directly.',
      'The policy is **stochastic**: it outputs a probability for each action. In the playground’s cart-pole, π_θ(push right | s) = σ(θᵀφ(s)) — logistic regression on the four state variables. Randomness provides exploration for free and makes the objective smooth in θ.',
      'The objective is J(θ) = E_τ~π_θ[R(τ)]: the expected total reward of trajectories τ generated by following the policy. We want ∇J(θ) to do gradient ascent — but the expectation is over trajectories whose distribution depends on θ, through an environment we cannot differentiate.',
      'Policy methods shine where value-based methods struggle: continuous states and actions (robot torques, portfolio weights), policies that must be stochastic, and very large action spaces — and they are the RL component of language-model fine-tuning (Lab 55).',
      'Procedure: choose a policy class that can express good behaviour, a reward that measures what you want (Lab 37’s reward hacking warning applies), and a baseline policy to compare against.',
    ],
    formula: 'J(θ) = E_{τ ~ π_θ}[Σₜ γᵗ rₜ]     π_θ(right | s) = σ(θᵀφ(s))',
    derivation: {
      title: 'A logistic policy',
      steps: [
        { prompt: 'With θᵀφ(s) = 0, what is the probability of pushing right?', number: 0.5, show: '`σ(0) = 0.5`' },
        { prompt: 'Write the probability of pushing left, 1 − σ(z), in terms of z.', answer: '1/(1+exp(z))', show: '`σ(−z) = 1/(1 + e^z)`', vars: { z: [-4, 4] } },
        { prompt: 'With z = 2, probability of pushing right? (Four decimals.)', number: 1 / (1 + Math.exp(-2)), tolerance: 1e-4, show: '`≈ 0.8808`' },
      ],
      result: 'The policy is a classifier whose “labels” come from rewards rather than from a teacher.',
    },
    experiment: 'In the cart-pole view, play the learned actor–critic policy. Watch the probability of pushing right as the pole tilts. What does the policy seem to respond to most?',
    question: 'A policy survives 34 steps in one episode with reward +1 per step and no discount. What is the episode’s return?',
    answer: 34,
    explanation: '34 × 1 = 34.',
    reflection: 'Name a decision problem with continuous actions where a value table would be hopeless.',
  },
  {
    id: 'l58-reinforce',
    title: '58.2 · The policy-gradient theorem and REINFORCE',
    sections: ['The log-derivative trick', 'No model of the environment needed', 'REINFORCE', 'Credit assignment with returns-to-go', 'Why it is noisy'],
    skill: 'Derive the REINFORCE estimator and explain its high variance.',
    prerequisite: 'Lesson 58.1; expectations (Lab 04).',
    paragraphs: [
      'The **log-derivative trick**: ∇_θ E_{x~p_θ}[f(x)] = E_{x~p_θ}[f(x)·∇_θ log p_θ(x)]. It turns the gradient of an expectation into an expectation we can estimate by sampling.',
      'For a trajectory, log p_θ(τ) = Σₜ log π_θ(aₜ | sₜ) + (terms from the environment that do not depend on θ). The environment terms vanish from the gradient: **∇J = E[Σₜ ∇log π_θ(aₜ | sₜ)·R(τ)]**. No model of the dynamics is needed.',
      '**REINFORCE**: run an episode, compute the gradient estimate, take a step. For the logistic policy, ∇log π(a | s) = (a − p)·φ(s): increase the probability of the chosen action in proportion to how well things went.',
      'An action cannot affect rewards received before it, so replace R(τ) by the **return-to-go** Gₜ = Σ_{k≥t} γ^(k−t) r_k — same expectation, less noise (the policy-gradient theorem in its usual form).',
      'The estimator is unbiased but very noisy: one episode’s return depends on luck in every later step. In the playground, plain REINFORCE learns on some seeds and stalls on others at the same learning rate.',
    ],
    formula: '∇J(θ) = E[Σₜ ∇log π_θ(aₜ|sₜ) · Gₜ]     logistic policy: ∇log π(a|s) = (a − p)·φ(s)',
    derivation: {
      title: 'The log-derivative trick',
      steps: [
        { prompt: 'Write ∇ log p in terms of p and ∇p (use dp for ∇p).', answer: 'dp/p', show: '`∇p / p`', vars: { dp: [-1, 1], p: [0.1, 0.9] } },
        { prompt: 'For the logistic policy with a = 1 (pushed right), d/dz log σ(z) = ?', answer: '1 - σ(z)', show: '`1 − σ(z) = a − p`', vars: { z: [-4, 4] } },
        { prompt: 'And with a = 0: d/dz log(1 − σ(z)) = ?', answer: '-σ(z)', show: '`−σ(z) = a − p`', vars: { z: [-4, 4] } },
      ],
      result: 'Both cases give (a − p): the same “prediction minus target” shape as logistic regression, weighted by return.',
    },
    experiment: 'In the cart-pole view, compare the three seeds of plain REINFORCE. How different are their learning curves?',
    question: 'At some step p = 0.7, the agent pushed right (a = 1), φ₃ = 0.5, and the return-to-go is 40. What is that step’s contribution to the gradient for θ₃?',
    answer: 6, tolerance: 1e-9,
    explanation: '(a − p)·φ₃·G = 0.3 × 0.5 × 40 = 6.',
    reflection: 'Why is it fine that REINFORCE never looks at the physics equations of the cart-pole?',
  },
  {
    id: 'l58-baseline',
    title: '58.3 · Baselines and advantages',
    sections: ['Subtracting what was expected', 'Why it stays unbiased', 'Choosing a baseline', 'The advantage', 'Measuring the effect'],
    skill: 'Reduce policy-gradient variance with baselines and explain why they keep the gradient unbiased.',
    prerequisite: 'Lesson 58.2; control variates.',
    paragraphs: [
      'Replace Gₜ by **Gₜ − b(sₜ)** for any baseline b that does not depend on the action. Actions are then judged by whether they did better or worse than expected, not by the raw return.',
      'It stays unbiased because E_a[∇log π(a | s)] = Σₐ ∇π(a | s) = ∇ Σₐ π(a | s) = ∇1 = 0: the baseline term has expected gradient zero. In the variance view, the gradient means with and without a baseline agree within sampling noise while the spreads shrink.',
      'A natural baseline is the **value** V(s) = E[Gₜ | sₜ = s]. Then Gₜ − V(sₜ) estimates the **advantage** A(s, a) = Q(s, a) − V(s): how much better this action was than the policy’s average action in that state.',
      'In the playground, REINFORCE with a learned linear baseline learns reliably on every seed where plain REINFORCE was hit-and-miss.',
      'The baseline must not use information from the chosen action or later rewards of the same episode — that would bias the gradient. The playground estimates its variance-demo baseline from separate episodes for this reason.',
    ],
    formula: '∇J = E[Σₜ ∇log π(aₜ|sₜ)·(Gₜ − b(sₜ))]     A(s, a) = Q(s, a) − V(s)',
    derivation: {
      title: 'The baseline term has zero mean',
      intro: 'A two-action logistic policy with p = P(right).',
      steps: [
        { prompt: 'Expected value of (a − p) under the policy: p·(1 − p) + (1 − p)·(0 − p). Simplify.', answer: '0', show: '`0`', vars: { p: [0.05, 0.95] } },
        { prompt: 'So E[(a − p)·φ·b] for a fixed state and baseline b equals?', number: 0, show: '`0`' },
      ],
      result: 'Any action-independent baseline changes the variance, never the expected direction.',
    },
    experiment: 'In the variance view, compare the gradient distributions for the pole-angle component. By how much does the standard deviation shrink?',
    question: 'Returns-to-go at one step across four episodes are 30, 50, 10 and 70; the baseline is their mean. What are the advantages? Give the sum of their absolute values.',
    answer: 80,
    explanation: 'Mean 40; advantages −10, 10, −30, 30; absolute sum 80. Half the actions now count as worse than expected.',
    reflection: 'In your own words: why does comparing to “what usually happens” make learning less noisy?',
  },
  {
    id: 'l58-ac',
    title: '58.4 · Actor–critic methods',
    sections: ['Learn the baseline as you go', 'Temporal-difference advantage', 'Two learners, one agent', 'The critic must be good enough', 'Modern variants'],
    skill: 'Implement a one-step actor–critic and diagnose critic failures.',
    prerequisite: 'Lessons 58.1–58.3; TD learning (Lab 37).',
    paragraphs: [
      'An **actor–critic** keeps two learners: the **actor** (the policy) and the **critic** (a value estimate V_w(s)). The critic replaces the Monte Carlo return with a bootstrapped one, as in Q-learning (Lab 37).',
      'After each step, the **TD error** δ = r + γV(s′) − V(s) estimates the advantage of the action just taken. The critic moves toward reducing δ; the actor moves its policy by δ·∇log π(a | s). Updates happen every step, not at the end of episodes.',
      'Bootstrapping trades variance for bias: δ depends on one reward and the critic’s estimate, so it is far less noisy than a full return — but only as accurate as the critic.',
      'That is exactly what the playground shows. The value of a cart-pole state depends on how far the pole and cart are from failing in either direction, so the critic uses squared terms. With a purely linear critic the same actor–critic fails completely; with quadratic features it balances the pole for all 200 steps within about 100 episodes on every seed.',
      'Modern policy-gradient algorithms build on this: A2C/A3C (many parallel actors), generalized advantage estimation (blending one-step and Monte Carlo advantages), and **PPO**, which clips each update so the policy cannot change too much at once — the workhorse behind RLHF for language models (Lab 55).',
    ],
    formula: 'δ = r + γV_w(s′) − V_w(s)     w ← w + β δ ∇V_w(s)     θ ← θ + α δ ∇log π_θ(a|s)',
    derivation: {
      title: 'One actor–critic update',
      intro: 'Reward 1, γ = 0.95, V(s) = 10, V(s′) = 12, p = 0.4, a = 1, and one feature φ = 0.5.',
      steps: [
        { prompt: 'TD error δ? (Two decimals.)', number: 1 + 0.95 * 12 - 10, tolerance: 1e-9, show: '`1 + 11.4 − 10 = 2.4`' },
        { prompt: 'Actor update direction for θ: δ·(a − p)·φ? (Two decimals.)', number: 2.4 * 0.6 * 0.5, tolerance: 1e-9, show: '`2.4 × 0.6 × 0.5 = 0.72`' },
      ],
      result: 'A positive TD error says “better than expected”: make that action more likely.',
    },
    experiment: 'In the cart-pole view, set training episodes to 100 and compare the three methods. Which one already balances reliably?',
    question: 'The critic predicts V(s) = 5 and V(s′) = 5; the reward is 1 and γ = 0.8. What is the TD error?',
    answer: 0, tolerance: 1e-9,
    explanation: 'δ = 1 + 0.8 × 5 − 5 = 1 + 4 − 5 = 0: the step went exactly as the critic expected, so the actor does not change.',
    reflection: 'What would you give a critic to look at, for a problem you know?',
  },
  {
    id: 'l58-practice',
    title: '58.5 · Policy gradients in practice',
    sections: ['Sample efficiency', 'Stability', 'Entropy and exploration', 'When not to use RL', 'Demonstrate understanding'],
    skill: 'Judge when policy-gradient RL is appropriate and how to make it stable.',
    prerequisite: 'Lessons 58.1–58.4; experiments and bandits (Labs 36, 50).',
    paragraphs: [
      'Policy gradients are **sample-hungry**: every update needs fresh episodes from the current policy (on-policy). That is cheap in simulators and expensive in the real world, where offline RL and careful simulation matter.',
      'Training is fragile: learning rates, reward scales and random seeds change outcomes (the playground’s plain REINFORCE). Normalize advantages, clip or constrain updates (PPO, trust regions), and always report several seeds (Lab 38).',
      'Stochastic policies can collapse to near-deterministic ones too early and stop exploring. An **entropy bonus** — adding β·H(π(· | s)) to the objective — keeps probabilities from hardening before the agent has learned enough.',
      'Do not reach for RL when the problem is really supervised (you know the correct action), a bandit (actions do not change future states, Lab 50), or solvable by planning with a known model (Lab 37’s value iteration). RL earns its complexity when actions have delayed, compounding consequences and a good simulator or safe exploration exists.',
      'You have completed this lab when you can: formulate a stochastic policy and its objective; derive REINFORCE with the log-derivative trick; explain and demonstrate variance reduction with baselines; implement an actor–critic and diagnose critic failures; and decide when policy gradients are the right tool. The Python challenge implements the policy gradient, a baseline and the TD error, with checks against exact expectations.',
    ],
    formula: 'objective with entropy bonus: J(θ) + β·E[H(π_θ(·|s))]     PPO: clip(π_new/π_old, 1 − ε, 1 + ε)',
    derivation: {
      title: 'Entropy of a two-action policy',
      steps: [
        { prompt: 'Entropy in bits of a policy that pushes right with probability 0.5?', number: 1, show: '`1 bit`' },
        { prompt: 'With probability 0.99? (Three decimals.)', number: -(0.99 * Math.log2(0.99) + 0.01 * Math.log2(0.01)), tolerance: 1e-3, show: '`≈ 0.081 bits`', why: 'Nearly deterministic: an entropy bonus would push back toward exploring.' },
      ],
      result: 'An entropy bonus rewards keeping options open while the policy is still learning.',
    },
    experiment: 'Across both views, write down one sentence each on bias, variance and stability for REINFORCE, REINFORCE with a baseline, and actor–critic.',
    question: 'Actions in your problem do not affect future states, and you observe the reward of the chosen action only. Which framework fits best?',
    choices: [
      { text: 'Full RL with policy gradients', why: 'There is no credit assignment over time to solve, so full RL adds variance and cost for nothing.' },
      { text: 'Contextual bandits', why: 'Simpler and more sample-efficient when actions do not change future states (Lab 50).' },
      { text: 'Supervised learning on the observed rewards', why: 'You only see the reward of the action you chose, so you also need exploration.' },
    ],
    answer: 1,
    explanation: 'Without effects on future states there is no credit assignment over time: contextual bandits (Lab 50) are simpler and more sample-efficient.',
    reflection: 'Which problem you know truly needs RL, and what would a safe simulator for it look like?',
  },
]

export const sources = [
  { title: 'Sutton & Barto · Reinforcement Learning: An Introduction, ch. 13 policy gradient methods (free book)', url: 'http://incompleteideas.net/book/the-book-2nd.html' },
  { title: 'Williams (1992) · Simple statistical gradient-following algorithms for connectionist reinforcement learning (REINFORCE)', url: 'https://doi.org/10.1007/BF00992696' },
  { title: 'Schulman et al. (2017) · Proximal policy optimization algorithms', url: 'https://arxiv.org/abs/1707.06347' },
  { title: 'OpenAI Spinning Up · Intro to policy optimization', url: 'https://spinningup.openai.com/en/latest/spinningup/rl_intro3.html' },
]
